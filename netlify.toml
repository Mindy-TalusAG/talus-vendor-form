[build]
  publish = "public"
  functions = "netlify/functions"

# Allow the form to be embedded in SharePoint via iframe
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "ALLOWALL"
    Content-Security-Policy = "frame-ancestors *"
